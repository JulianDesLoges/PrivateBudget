@using PrivateBudget.Client.Models;
<div class="category-color" style="background-color:@(Category?.Color ?? "transparent")" />@(Category?.Name ?? "No category")

@code {
    [Parameter]
    public Category? Category { get; set; }
}
